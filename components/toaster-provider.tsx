"use client"

// Importing Toaster for notification display
import { Toaster } from "react-hot-toast"

// ToasterProvider component to render the Toaster
export const ToasterProvider = () =>{
    return <Toaster />
}